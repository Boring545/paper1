
4.3.1 冗余信号生成与打包算法`n`t第三章中的信号打包算法是在信号集合已经确定的前提下，将信号映射到 CAN FD 报文集合中，并以带宽利用率和可调度性作为评价依据进行模拟退火优化，其算法输出是信号到报文的映射关系。本章引入按需三模冗余后，待优化对象不再只是报文装载关系，还包括冗余功能路的生成与副本源 ECU 的选择。`n`t设原始信号集合为S0，其中部分安全关键信号需要采用按需三模冗余保护。对于某一需要保护的信号，其功能冗余簇为Ωq=Gq, g0,Gq, g1,Gq, g2。对于给定的原始功能路Gq, g0，算法需要生成两个异源功能路信号组Gq, g1和Gq, g2。生成后的副本信号在周期、截止时间、长度和安全等级上与原信号保持一致，但其通信冗余标识id和源 ECU 不同。`n`t为保证三条功能路不会被同一 ECU 节点失效同时影响，三条功能路上的信号必须满足源 ECU 隔离约束。同时，一条功能路上的副本源 ECU 的改变必须以信号组为单位整体进行。若仅改变组内部分信号的源 ECU，则同一功能路内部的信号将不再对应同一个功能输出路径。因此，在冗余信号生成和后续打包算法的邻域扰动中，一个信号组内所有信号的源ECU 必须保持一致。`n`t待生成的信号包括主功能路的Gq, g1和备份功能路的Gq, g2中的信号，算法采用启发式策略获得两条功能路的源 ECU。该策略的基本思想是：在满足源 ECU 隔离约束的前提下，优先选择有利于后续信号合包的 ECU。具体而言，对于某一待生成的功能路，算法首先在候选 ECU 中查找是否已经存在与该功能路具有相同周期、相同目的 ECU 的信号。若存在这样的 ECU，则将该功能路部署到该 ECU 上，有利于其在后续打包过程中与已有信号合并到同一 CAN FD 报文中，从而减少新增报文数量和带宽开销。该启发式策略仅用于构造模拟退火的初始解，后续退火过程中仍可在邻域扰动过程调整源ECU的位置`nAlgorithm 4-1 副本源 ECU 初始选择策略`n`n输入：原始功能路 G_{q,g_0}，ECU 集合 E，当前信号集合 S`n输出：主功能路 G_{q,g_1} 和备份功能路 G_{q,g_2} 的源 ECU`n`n1: 令 E_0 = μ(G_{q,g_0})`n2: 对于待生成的功能路 G_{q,g_1}：`n3:     构造候选 ECU 集合 C_1 = E - {E_0}`n4:     在 C_1 中查找存在相同周期、相同目的 ECU 信号的 ECU`n5:     若存在，则选择其中负载最低的 ECU 作为 μ(G_{q,g_1})`n6:     否则，选择 C_1 中负载最低的 ECU 作为 μ(G_{q,g_1})`n7: 对于待生成的功能路 G_{q,g_2}：`n8:     构造候选 ECU 集合 C_2 = E - {E_0, μ(G_{q,g_1})}`n9:     在 C_2 中查找存在相同周期、相同目的 ECU 信号的 ECU`n10:    若存在，则选择其中负载最低的 ECU 作为 μ(G_{q,g_2})`n11:    否则，选择 C_2 中负载最低的 ECU 作为 μ(G_{q,g_2})`n12: 返回 μ(G_{q,g_1}) 和 μ(G_{q,g_2})`n-------------------------------`n`t完成副本生成后，算法构造全量信号集合:`nSall=S0∪Smain∪Sbak`n`t其中，S0是原始信号集合，Smain表示为按需三模冗余生成的第二主功能路信号集合，Sbak表示备份功能路信号集合。打包算法针对Sall按照3.3章的启发式方法生成初始的周期性报文集合Fall。Fall表示离线配置阶段生成的最大报文集合，而不是任意运行状态下都会全部发送的报文集合。系统在线运行时，需要根据当前状态选择实际参与发送的报文。`nSN=S0∪Smain`n`t由此可得到正常态实际发送的周期性报文集合FN：`nFN={fj∈Fall | fj中至少包含一个属于SN的发送信号}`n`t若某一报文只包含备份功能路信号，则该报文在正常态下不发送；若某一报文同时包含正常态信号和备份功能路信号，则仍然正常发送。`n`t在最坏的故障模式下，备份功能路被激活，周期性信号集合扩展为：`nSW=S0∪Smain∪Sbak`n`t由于最坏情况分析中假设所有采用按需三模冗余保护的功能均触发备份功能路发送，因此全量打包方案中的周期性报文均可能参与总线竞争。此时最坏故障模式下的周期性报文集合可直接表示为Fall。`n`t此外，模式切换过程中还会发送若干事件触发报文，例如备份激活报文、隔离报文和备份挂起报文：`nFevt={fact,fiso,fhang}`n`t其中fact用于通知备份功能路从挂起状态切换为发送状态，fiso用于通知异常功能路停止发送或在接收端将其输出排除在表决输入之外，fhang用于在恢复完成后通知备份功能路重新进入挂起状态。由于这类报文只承载模式切换控制信息，不承载具体功能数据，本文将其负载长度统一设为 CAN FD 的最小有效负载长度，即1 byte。`n`t事件触发报文不是周期性功能报文，因此本文不为其定义普通意义上的发送周期，而采用最小到达时间间隔 MIT 描述其在最坏响应时间分析中的最大出现频率。对于功能冗余簇Ωq，其事件触发报文的最小到达时间间隔设为该功能冗余簇对应原始信号的周期tq。在优先级分配时，把MIT作为事件触发报文的虚拟周期，并按照第三章的方法为其分配合适的优先级。`n`t综上，最坏故障模式下参与实时性分析的总报文集合为：`nFW=Fall∪Fevt`n`t通过该定义，最坏故障模式同时覆盖了全量周期性功能报文负载和模式切换带来的额外开销。`n`t在上述全量信号集合的基础上，本文仍采用第三章中的模拟退火打包框架。与第三章不同的是，本节的打包方案不只记录信号到报文的映射关系，还隐含记录了冗余功能路的源 ECU 选择结果。`n`t模拟退火对候选打包方案的修改既可能改变信号在报文中的位置，也可能改变某条功能路整体的源 ECU，从而实现副本部署和报文打包的联合优化。在该框架下，对解的邻域扰动分为两类。第一类是3.3章的报文装载扰动，该类扰动只改变信号到报文的映射关系。第二类为功能路源 ECU 扰动，该扰动随机选择某个需要按需三模冗余保护的信号，选择其第二主功能路或备份功能路对应的信号组，将该信号组整体迁移到另一个满足约束的候选 ECU 上。`n`t设信号的源 ECU为Ea，经第二类扰动后新的源ECU为Eb，则该扰动可表示为：`n∀si∈Gq, gx,srcsi:Ea→Eb,x∈{1,2}`n`t该操作必须以功能路对应的信号组为单位整体执行，不能只修改组内部分信号，迁移后需要重新检查源 ECU 隔离约束。由于源 ECU 的改变会影响信号能够装载的报文集合，因此迁移过程不能只修改信号属性，还需要对当前打包方案执行局部重构。算法首先将Gq, gx中的所有信号从原报文中移除；若原报文因此为空，则删除该报文；随后将该信号组的源 ECU 修改为Eb并对信号进行迁移，并对这些信号重新执行3.3章中的插入到已有报文和新建报文操作。`n`t信号组Gq, gx内可能包含多个同源信号副本，这些副本仍需要满足的副本分离约束，不能被装载到同一个CAN FD 报文中。因此，在局部重打包过程中，对于任意两个同属于Gq, gx的的同源副本si和sj仍需满足`nmsi≠msj`n`t通过交替执行上述两类扰动，算法既可以调整信号到报文的映射关系，也可以调整冗余功能路的源 ECU，从而实现副本 ECU 选择与信号打包的联合搜索。`t在扰动后新的候选方案满足基本约束的基础上，算法进一步从正常模式和最坏故障模式两个角度评价其通信开销。正常模式下的带宽利用率可表示为：`nUN=fj∈FNCjTj`n`t最坏故障模式下，备份功能路被激活，系统需要考虑三路表决态、降级运行态或恢复判定态中可能出现的通信负载。此时，原始功能路、第二主功能路、备份功能路以及模式切换过程中产生的事件触发报文均可能占用总线资源。最坏故障模式下的带宽利用率可表示为：`n`nUW=fj∈FallCjTj+fk∈FevtCkMITk`n`tMITk表示事件触发报文fk的最小到达间隔。由于故障模式持续时间通常短于正常模式，算法不将UF作为主要优化目标，而是将其作为辅助优化项和约束评价依据，用于避免故障触发后总线负载过高。`t`n`t除带宽利用率外，候选方案还需要满足可调度性约束。由于最坏故障模式是在正常模式基础上激活所有的备份功能路，等价于退化为三模冗余的状态，并额外叠加模式切换的事件触发报文形成的保守负载场景。在分配优先级时，会对FW中的所有报文统一分配，这使得最坏故障模式下的干扰不会小于正常模式。若候选打包方案在最坏故障模式下满足可调度性约束，则正常模式下的可调度性可以由此得到保证。因此，本文不再单独设置正常模式可调度性惩罚，而是采用最坏故障模式下的可调度性惩罚函数：`nSchedW(Π)=0,  Π 在最坏故障模式下可调度1,  Π 在最坏故障模式下不可调度`n`t因此，在按需三模冗余场景下，打包方案 Π 的评价函数可以在第三章的基础上扩展为：`nfitnessΠ=UN+λUW+SchedWΠ`n`t其中，λ为故障模式带宽权重。由于系统大部分时间处于正常运行状态，正常模式带宽UN是主要优化对象；最坏故障模式带宽UW用于约束备份功能路激活后系统的通信开销，防止故障模式下总线负载过高。可以设0&lt;λ&lt;1，使算法优先降低正常模式下的带宽利用率，同时兼顾故障模式下的通信资源占用。`n`t后续的接受准则、温度衰减方式和历史最优解更新规则沿用3.3节的的模拟退火过程。只有当候选方案在最坏故障模式下满足可调度性约束时，才允许其作为历史最优解的候选。算法最终输出历史最优可行打包方案Πbest。从打包方案中可以得到每个按需三模冗余的信号的第二主功能路和备份功能路对应的信号组的源 ECU 选择结果。`n`t本节提出的联合打包算法仍需要嵌入第三章的迭代优化流程中。第三章通过不断计算信号组通信失效概率，并对不满足功能安全阈值的信号组增加同源备份，以保证信道传输可靠性。本章在此基础上进一步引入异源功能路和按需三模冗余，用于处理 ECU 或任务节点输出不可信的问题。`n`t该流程的伪代码为：`nAlgorithm 4-2 按需三模冗余下的迭代打包流程`n`n输入：原始信号集合 S^0，ECU 集合 E`n输出：最优可行打包方案 Π_best`n`n1: 为需要保护的信号生成 G_{q,g_1} 和 G_{q,g_2}；`n2: 采用算法 4-1 选择副本源 ECU；`n3: 构造全量信号集合 S^{all}；`n4: 执行联合模拟退火打包，得到方案 Π；`n5: 检查 Π 在最坏故障模式下的可调度性；`n6: 计算各功能路 G_{q,g_x} 的通信失效概率；`n7: 若所有功能路均满足安全阈值，则输出 Π；`n8: 否则，对不满足阈值的功能路增加同源信号副本；`n9: 更新 S^{all}，返回步骤 4。`n`n4.3.2 模式切换下的实时性分析`n`t在按需三模冗余机制下，系统在正常态和故障触发态下具有不同的通信行为。正常态下，功能冗余簇Ωq中仅原始功能路Gq, g0和第二主功能路Gq, g1参与周期性发送；当两条主功能路输出不一致时，目的 ECU 需要先完成一致性判定，再发送备份激活报文，随后由备份功能路发送第三路输出并进行多数表决。因此，模式切换下的实时性分析需要在第三章信号组最坏响应时间的基础上，进一步区分正常态下的信号组 WCRT 与备份激活后的端到端最坏响应时间。`n`t对于正常态下任意报文fj∈FN，其最坏响应时间记为 RjN。设hpfj表示表示正常态下优先级高于fj的周期性报文集合，则报文fj在正常态的最坏响应时间可由如下递归式计算：`nRjN,(n+1)=Cj+Bj+fk∈hp(fj)RjN,(n)TkCk`n`tCj表示报文fj在CAN FD 总线上的最坏传输时间，Bj表示低优先级报文的最大阻塞时间，递归的初始状态为RjN,(0)=Cj+Bj。`n`t对于最坏故障模式下的的任意报文fj∈FW，其最坏响应时间记为RjW。由于FW既包含周期性功能报文，也包含备份激活、隔离和备份挂起等事件触发报文，因此在计算报文级最坏响应时间时，需要同时考虑周期性报文干扰和偶发事件报文干扰。则报文fj在最坏故障模式下的最坏响应时间可由如下递归式计算：`nRjW,(n+1)=Cj+Bj+fk∈hpper(fj)RjW,(n)TkCk+fh∈hpevt(fh)RjW,(n)MIThCh`n`thpper(fj)表示优先级高于fj的周期性功能报文集合，hpevt(fj)表示优先级高于fj的事件触发报文集合，MITh表示事件触发报文fh的最小到达间隔。递归的初始状态为RjW,(0)=Cj+Bj。`n`t由于最坏故障模式的报文集合包含正常态的周期性报文，并额外叠加备份功能路报文和事件触发报文，因此通常有：`nRjN≤RjW`n`t在报文级 WCRT 计算完成后，需要进一步计算信号组或功能路的最坏响应时间。对于普通信号组 G，其正常态和最坏故障模式下的信号组的最坏响应时间分别定义为：`nRGN=maxsi∈GRm(si)N`nRGW=maxsi∈GRm(si)W`n`t对于未配置按需三模冗余保护的普通信号组，虽然其本身不会触发备份功能路，但在最坏故障模式下，其所在报文仍可能受到备份功能路报文和事件触发报文的干扰。因此，普通信号组也需要基于RGW判断模式切换干扰下的实时性。`n`t设普通信号组G对应的超时时间为dG，则G在模式切换干扰下的可调度性约束为：`nRGW≤dG`n`t对于配置按需三模冗余保护的功能冗余簇Ωq=Gq, g0,Gq, g1,Gq, g2，每一条功能路本质上也是一个信号组。三条功能路在正常态和最坏故障模式下的最坏响应时间为：`nRq,xN=maxsi∈Gq, gxRmsiN, x∈{0,1}`nRq,xW=maxsi∈Gq, gxRmsiW, x∈{0,1,2}`n`t正常态下，只有两条主功能路参与发送和一致性比较。两条主功能路阶段的等待时间应取两条功能路 WCRT 的最大值：`nRqmain,W=maxRq,0W,Rq,1W`n`t设tcmp表示目的 ECU 完成两路一致性判定所需的最坏处理时间。则功能冗余簇Ωq在正常态下的端到端最坏响应时间可表示为：`nRΩqN=Rqmain,W+tcmp`n`t在故障模式下，信号经过按需三模冗余检验的端到端时延是一个串行过程：两条主功能路先发送并到达目的 ECU；目的 ECU 完成一致性判定后，若发现两路输出不一致，则发送备份激活报文；备份 ECU 接收激活报文后完成后台任务唤醒和本地计算；最后备份功能路发送第三路输出，并由目的 ECU 完成多数表决。`n`t设备份激活报文的fqact的最坏响应时间为Ract,qW, 备份功能路对应的进程在收到激活报文后完成后台任务唤醒和备份计算的最坏时间为tqbak，备份功能路上Gq, g2的最坏响应时间为Rq,2W，则功能冗余簇Ωq在模式切换触发路径下的端到端最坏响应时间可表示为：`nRΩqW=Rqmain,W+tcmp+Ract,qW+tqbak+Rq,2W`n`t设功能冗余簇Ωq对应信号的截止时间为dq，则正常态和模式切换触发路径下的可调度性约束分别为：`nRΩqN≤dq`nRΩqW≤dq`n`t由于最坏故障模式下的总线负载包含正常态流量，并额外叠加备份功能路和事件触发报文干扰。若RΩqW≤dq成立，则通常可以推出正常态下的实时性要求也被满足。`n`t对于未配置按需三模冗余保护的普通信号组，其实时性分析仍沿用第三章中的信号组最坏响应时间的定义。虽然这类信号组本身不触发备份功能路，但其所在报文在最坏故障模式下仍会受到备份功能路报文以及事件触发报文的干扰。因此，信号组G的最坏响应时间也应基于最坏故障模式下的报文响应时间RjW计算：`nRGW=maxsi∈GRm(si)W`n`t设信号组G对应的超时时间为dG，则普通信号组在模式切换干扰下的可调度性约束为：`nRGW≤dG`n`t该约束用于保证普通信号即使不参与按需三模冗余，也不会因其他安全关键功能触发备份功能路和事件触发管理报文而破坏自身实时性。`n`t综上，候选打包方案只有在所有普通信号组和所有功能冗余簇都满足可调度性约束时，才可认为满足模式切换下的实时性要求。`n
